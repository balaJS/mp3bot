var mp3bot = {};

const msf_songs = new MSFmultiSelect(
  document.querySelector('.multi-select'),
  {
    theme: 'theme2',
    // appendTo: '.multi-select',
    selectAll: true,
    searchBox: true,
  }
);
